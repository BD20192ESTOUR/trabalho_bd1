SELECT USUARIO.NOME, COUNT(EVENTO.IDPONTOTURISTICO) FROM USUARIO INNER JOIN PONTOTURISTICO ON USUARIO.IDUSUARIO = PONTOTURISTICO.IDUSUARIO INNER JOIN EVENTO ON PONTOTURISTICO.IDPONTOTURISTICO = EVENTO.IDPONTOTURISTICO GROUP BY USUARIO.NOME;